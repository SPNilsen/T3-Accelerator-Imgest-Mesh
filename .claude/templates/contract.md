# Contract: {feature name}

> Generated by Spec Architect interview — confirmed by Creator on {date}.

## What It Does
{1-2 sentences — the anchor from Pass 1}

## Who It's For
{End user / developer / internal system / admin}

## User Flow
1. {First action — click, call, trigger}
2. {What comes back — shape, content}
3. {Next step — side effect, navigation, update}
4. {End state — what's different when done}

## Rules
- {Valid input constraints}
- {Rejection behavior}
- {Limits — max, min, rate}
- {Permissions — who can, unauthorized response}
- {Business logic — if X then Y}

## Integration Points
- **Reads from**: {DB tables, APIs, config, cache}
- **Writes to**: {DB tables, cache, queue, events}
- **Called by**: {UI component, route, scheduler, webhook}
- **Calls**: {external APIs, internal services, utilities}
- **Dependency failure**: {retry / degrade / fail hard / queue}

## Failure Handling
| Scenario | Expected Behavior |
|----------|-------------------|
| {bad input} | {specific error response} |
| {zero case} | {graceful handling} |
| {extreme case} | {limit enforcement} |
| {dependency down} | {fallback behavior} |

## Test Scenarios

### Valid Cases
| # | Input | Expected Output | Why This Matters |
|---|-------|-----------------|------------------|
| 1 | {diverse input A} | {exact output} | {tests core logic} |
| 2 | {diverse input B} | {exact output} | {tests different path} |
| 3 | {diverse input C} | {exact output} | {tests edge position} |

### Invalid Cases
| # | Input | Expected Behavior |
|---|-------|-------------------|
| 1 | {invalid input A} | {specific error/rejection} |
| 2 | {invalid input B} | {specific error/rejection} |

### End-to-End Scenario
{Real-world walkthrough: User X does Y, then Z. System should show W. State should be Q.}

## Severity Map
- **Critical** (must not fail): {list}
- **Important** (should not fail): {list}
- **Nice-to-have** (cosmetic): {list}

## Validation Holdbacks
{2-3 extra test scenarios NOT included in the initial test suite — reserved for the post-build validation pass. The Builder never sees these.}
