# Wiring — Local Dev (Windows / Docker Desktop)

Canonical doc lives in the project tree: [`docs/dev/local-dev.md`](../../../docs/dev/local-dev.md).

This pointer exists so Spark sessions can find the wiring guide via the
`.claude/reference/wiring/` convention without duplicating content.
